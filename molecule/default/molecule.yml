---
dependency:
  name: galaxy
  enabled: False
driver:
  name: docker
platforms:
  - name: instance
    image: ${MOLECULE_DISTRO:-ubuntu:20.04}
    groups:
      - openvpnclient
      # env:
      #   http_proxy: ${http_proxy}
      #   https_proxy: ${https_proxy}
      #   no_proxy: ${no_proxy}
provisioner:
  name: ansible
  config_options:
    defaults:
      verbosity: 1
scenario:
  name: default
verifier:
  name: ansible
